spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    #url: jdbc:postgresql://warehouse-database-1:5432/${DATABASE_NAME}
    url: jdbc:postgresql://localhost:5432/${DATABASE_NAME}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
jwt:
  secret: ${JWT_SECRET}